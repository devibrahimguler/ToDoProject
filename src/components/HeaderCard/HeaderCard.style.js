import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        flexDirection: "row",
        justifyContent: "space-between",
        marginRight:15,
        marginHorizontal: 10,
        marginVertical: 5,
    },
    title:{
        fontSize: 40,
        fontWeight: "bold",
        color: "#F2A93B",
    }
});